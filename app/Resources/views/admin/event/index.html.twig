{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_event_index' %}

{% block main %}
    <h1>{{ 'title.event_list'|trans }}</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Заголовок</th>
                <th><i class="fa fa-calendar"></i> Начало</th>
                <th><i class="fa fa-cogs"></i> Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for event in events %}
            <tr>
                <td>{{ event.title }}</td>
                <td>{% if event.startTime %}{{ event.startTime|date('Y-m-d H:i') }}{% endif %}</td>
                <td>
                    <div class="item-actions">
                        <a href="{{ path('admin_event_show', { id: event.id }) }}" class="btn btn-sm btn-default">
                            {{ 'action.show'|trans }}
                        </a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block sidebar %}
    <div class="section actions">
        <a href="{{ path('admin_event_new') }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-plus"></i> {{ 'action.create_event'|trans }}
        </a>
    </div>

    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
